LITE RELEASE — tag: lite-1.0.0
==============================

Put this 1 file in THIS folder:

  1) Client.dll

(Lite does NOT use native hooks — no .so file.)

Upload to GitHub:
  Repo → Releases → lite-1.0.0 → Edit → attach Client.dll

Asset name on GitHub MUST be exactly:
  Client.dll

After upload, run from repo root:
  powershell -File Scripts\Update-ManifestHashes.ps1 -Channel lite -Version 1.0.0

Loader saves downloaded file to:
  internal\lite\Client.dll
